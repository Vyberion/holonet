# Temporary Routes

Routes in this group are deliberately kept separate from the main Holonet application tree.

Next.js route groups do not change the public URL, so these pages can remain reachable while still being visibly marked as temporary in the file structure.
